ame: Configure
runs:
  using: composite
  steps:
    - name: Restore Test
      uses: actions/cache/restore@v3
      with:
        path: foo
        key: foo
    - name: Shell Test
      shell: bash
      run: |
        echo "Hello World"
